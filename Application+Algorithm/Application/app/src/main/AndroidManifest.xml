<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_temp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_temp_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Application.NoActionbar">
        <activity
            android:name=".graphic.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Application.NoActionbar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".graphic.GameActivity"
            android:label="GameScreen"
            android:theme="@style/Theme.Application.NoActionbar"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>